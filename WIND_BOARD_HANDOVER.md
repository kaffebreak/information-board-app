# 引継ぎ — 風向・風速モニタ（名古屋／四日市）

防災・交通情報モニタ（`bousai_board.py`）に続く2枚目のボード。事務所の3画面のうち**モニタ3（左端）**に
キオスクで常時表示する。スクロール・クリックなし。

この文書は実装をゼロから引き継ぐためのもの。**画面仕様は確定、データ源の仕様も実物で確認済み**。
未確認事項は最後の章にまとめてある。

---

## 1. 置き場所と構成

既存リポジトリ `kaffebreak/information-board-app` に**2枚目のボードとして追加**する（別リポジトリにしない）。
配色・フォント・4段階の鮮度表現・ログ・キオスク起動をそのまま流用できるのが理由。

```
bousai_board.py / bousai_board.html   既存（触らない）
kaze_board.py   / kaze_board.html     ← 今回追加
kaze_history.json                     ← 追加。風配図用の蓄積（.gitignore に入れる）
config.json                           ← wind_port などを追記
start_board.bat                       ← 2画面分に拡張
```

`bousai_board.py` の作法を踏襲する。**Python 3.9+ / 標準ライブラリのみ**（外部ライブラリを増やさない）。

- `src_*()` を観測点ごとに1つ、スレッドで `interval` ごとに実行して `STATE` に保持
- `build_status()` が `/api/status` のJSONを組み立てる
- 取得失敗時は前回値を残す。画面へは `failing` / `partial` / `unknown` / `stale` の4段階で伝える
  （語彙は既存と揃える。ただし閾値は後述のとおり**観測間隔ベース**にする）
- ログは風向専用の `kaze_board.log`（1MB×3世代）。Windowsでのローテーション競合を避け、防災側と共有しない
- HTML側は15秒ごとに `/api/status` を取得。**通し番号で「最後に描画したものより古い応答」だけを捨てる**
  （既存と同じ理由。「次の取得が始まったら捨てる」にしてはいけない）
- ポートは既存と別にする。`start_board.bat` は各サーバーが応答するまで待ってから開く（既存と同じ理由。
  未起動のままキオスクを開くとエラー画面で固まり、自動更新も動かない）

---

## 2. データ源

| 観測点 | URL | 更新 | 履歴 |
|---|---|---|---|
| 名古屋港高潮防波堤 | `www6.kaiho.mlit.go.jp/nagoyako/kisyou/nagoyako_bw.html` | 15分 | **6時間分・25行** |
| 四日市防波堤信号所 | `www6.kaiho.mlit.go.jp/04kanku/yokkaichi/yokkaichiko_bkw_lt/kisyou/index.html` | 30分 | **12時間分・25行** |

どちらも海上保安庁。**サーバーレンダリングのHTMLで、JSONもJSも使っていない**（2026-09-16 に実物で確認）。
`html.parser` で表を読む。

### 表の構造（両方とも同じ）

列は `日付 / 時刻 / 風向 / 風速`。キャプションに観測時刻が入る。

- 名古屋：`観測時刻 2026/09/16 13:40`、観測点名は `名古屋港高潮防波堤中央堤東端`
- 四日市：`気象情報（最新12時間）観測時刻 2026/09/16 13:25`

実物の行（verbatim）:

```
名古屋   2026/09/16 | 13:40 | -    | 風弱く
        2026/09/16 | 13:25 | -    | 風弱く
        2026/09/16 | 12:40 | 南南西 | 3 m
四日市   2026/09/16 | 13:25 | 南南東 | 2 m
        2026/09/16 | 11:55 | 南東   | 2 m
        2026/09/16 | 01:25 | 北北西 | 2 m
```

### 表記規則と落とし穴

- **風向は漢字16方位**（`南南西` `南東` `北北西`）。**度数は出ない**。矢印の角度は16方位テーブルで換算する
  （北 0° から 22.5° 刻み）
- **風速は整数＋半角スペース＋`m`**（`3 m`）。**小数は出ない**。単位文字がセルに含まれるので剥がす。
  画面表示も整数にする（`8.0` のように見せると出ていない精度を主張することになる）
- **弱風時は 風向 `-` ／ 風速 `風弱く`**。数値が来ない状態が**通常運用でそれなりの頻度で起きる**
  （確認した 13:40 時点で名古屋は4サンプル連続で `風弱く` だった）。これは欠測ではなく正常な観測結果なので、
  `stale` 扱いにしてはいけない。専用の表示状態として扱う（§3）
- **最大瞬間風速は両ページに存在しない**。取れるのは風向と風速の2つだけ。
  依頼者の言う「最大風速」は**期間内の風速の最大値**の意味なので、6時間の系列から自分で求める
- **行の並びが時刻降順とは限らない**（四日市で `01:25` の次に `01:55` が来ていた）。
  **必ず日付＋時刻でパースしてソートしてから使う**
- **日付列を無視しない**。四日市は12時間分あるので日付をまたぐ
- 四日市には出来合いのグラフGIFがある：
  `www6.kaiho.mlit.go.jp/kishogenkyo/graphs/181_fuukou.gif` / `181_fuusoku.gif`。
  **局番`181`で `/kishogenkyo/` 配下に共通の気象現況システムがありそう**なので、
  名古屋側の局番と、HTMLより素直なデータ源がないかを**最初に調べる**価値がある（見つかればパーサが不要になる）
- ETag / Last-Modified が返るなら `http_get(conditional=True)` で差分取得する（既存と同じ）

### 風配図用の蓄積が必要

**24時間の風配図はページから取れない**（名古屋は6時間分しか公開していない）。
`kaze_history.json` に観測値を蓄積してリングバッファで24時間ぶん保持する。

- 起動直後は24時間に届かないので、**実際に集計できた期間を画面に出す**
  （「直近24時間」と書いてあるのに6時間ぶんしかない、が一番まずい）
- 重複行は日付＋時刻＋観測点でユニークにする
- `風弱く` のサンプルは風向不明なので**風配図の母数に入れない**。ただし「風弱く率」は別に持っておくと後で使える

---

## 3. 画面仕様（確定）

1920×1080固定。デザインのアートボードは Claude Design のキャンバス「名古屋・四日市 風向風速ボード」
ページ「A案（採用）」を参照。以下はそこから採った実寸。

### 共通

- 外枠 `padding: 18px`、縦 `gap: 12px`
- 配色は既存 `bousai_board.html` の `:root` と同一：
  `--bg:#0F2536` `--panel:#14304A` `--rule:#27506B` `--ink:#E8EEF1` `--mute:#89A3B3`
  `--ok:#46B98C` `--caution:#F0B429` `--alarm:#E5484D` `--stale:#7B8A95`
- 追加色（風速用の1色順次スケール。既存の状態色とは別枠にする）：
  `〜5 m/s #4A87AD` / `5〜10 #6FBBE0` / `10〜 #A8DDF5`。
  グラフは面 `#4FA8D8` 不透明度35%、線 `#A8DDF5` 3px
- フォントは `"BIZ UDPGothic", "Yu Gothic UI", "Yu Gothic", Meiryo, "Hiragino Sans", "Noto Sans JP", sans-serif`
- 数字は `font-variant-numeric: tabular-nums`（桁が動くと目が滑る）

### ヘッダー（高さ60px）

左に `風向・風速`（31px/700）＋ `名古屋港高潮防波堤／四日市防波堤信号所`（19px/mute）、
右に**日付のみ**（21px/mute）。

**時計は置かない。**モニタ1の防災ボードに常時出ているため。日付は日を跨いだ固着に気づくために残す。

### 本体（2カラム grid、gap 14px）

パネル1枚の縦配分（`padding: 14px 20px 16px`、内側 `gap: 12px`）:

| 行 | 高さ | 内容 |
|---|---|---|
| 局名行 | 46px | 局名38px/700 ＋ 観測点名・更新間隔17px/mute ／ 右にランプ＋`観測 13:40（3分前）`18px |
| 現況行 | 334px | 左：風配図296×296 ＋ 凡例 ＋ 注記／右：風向66px・風速**152px** |
| 統計行 | 112px | タイル2枚（`6時間最大風速` ＋ 時刻／`6時間平均`）。数字60px |
| 推移 | 残り | セクション見出し24px ＋ グラフ895×348 |

### 現況の表示状態

| 状態 | 風向 | 風速 | 風配図 |
|---|---|---|---|
| 通常 | 漢字16方位 66px/700 | 整数 **152px/700** ＋ `m/s` 40px。下に `3.9 kt ・ 風力 2`（24px/mute） | 矢印あり |
| 風弱く | `—` 66px（`--stale`色） | **`風弱く` 108px/700**（単位なし）。下に `観測値なし（風向・風速とも）`（22px/`--stale`） | **矢印なし**。中心の丸を白抜きにする |

`kt = m/s × 1.94384`、風力階級はビューフォート表。どちらも整数の風速から導出した値なので、
`kt` は小数1桁、風力は整数で出す。

### 風配図（296×296）

- 中心 `(148,148)`、内径 `26`、外径 `112`、**外周 = 15%**
- 16方位 × 3速度階級の積み上げ扇形（扇の半幅 9.2°）。集計は直近24時間
- **矢印＝風上（風の吹いてくる方向）**。凡例に明記する（取り違えが一番怖いので省略しない）
- 過去6時間の風向を薄い白線（不透明度 0.09→0.32）でゴースト表示。振れの向きと幅が読める
- 方位ラベルは漢字（`北` `東` `南` `西`）。北だけ `--ink`、他は `--mute`
- 注記：`風配図 直近24時間・外周15%／矢印＝風上` ← **実際の集計期間に差し替えて出す**

### 推移グラフ（895×348）

- Y軸は **0〜20 m/s 固定**。グリッド 0/5/10/15/20。目盛は左 `x=38` 右揃え
- **固定スケールにする**のが要点。風速3のときにグラフが下に張り付いて上が空くのは正しい表示で、
  「しきい値からどれだけ遠いか」がその空白で読める。オートスケールにすると弱風の日に波形が暴れて誤読を招く
- 面＋線＋各サンプルに点。**6時間最大風速の点に白丸（r=8）と数値**を重ねる
- しきい値 15 m/s に琥珀の破線。ラベルは**左端**（`x=52`）に置く。右端はデータと重なる
- 上端に風向の矢羽（毎時、`translate(x,26) rotate(方位角)`）
- **時間軸は局ごとに独立**。それぞれの最終観測時刻から遡って6時間。目盛は毎時の丸い時刻
  （名古屋の観測は :10 :25 :40 :55、四日市は :25 :55 なので、軸ラベルと点は一致しない。それでよい）
- **四日市は30分値のまま点を粗く描き、補間しない**。スプラインで名古屋と同じ密度に見せてはいけない
- `風弱く` は 0 として描く。セクション見出しの右に `15分値・風弱く＝0として描画` と明記する

### フッター（高さ24px）

左 `データ取得 正常（2系統）`（既存と同じ4段階の語彙）、右 `出典：海上保安庁`。

---

## 4. データ鮮度の判定

既存の `interval*3`・最低180秒ルールではなく、**観測間隔ベース**にする。観測が15分／30分と粗いので、
共通の秒数では四日市が常に古く見えてしまう。

| 観測点 | 正常 | caution（黄ランプ・「取得再試行中」） | stale（灰・破線ランプ・「判定できません」） |
|---|---|---|---|
| 名古屋 | 最終観測から20分以内 | 20〜35分 | 35分超 |
| 四日市 | 最終観測から35分以内 | 35〜65分 | 65分超 |

- 局名行には**常に `観測 HH:MM（n分前）` を出す**。四日市は `・次 HH:MM` も添える。
  更新間隔が違うことが画面から読めないと、古い四日市の値を現在値と読んでしまう
- `stale` のとき、数字は残したまま `--stale` 色に落とし、ランプを破線にする
  （消すと「風がない」と読まれるので消さない）
- **`風弱く` は鮮度の問題ではない**。正常な観測結果として扱う

---

## 5. 最初にやること（この順番）

1. 両ページを1回取得して、`html.parser` で表を抜くパーサを書く。`python -c "import kaze_board as k; print(k.src_nagoya())"`
   で単体確認できるようにする（既存 `src_*()` と同じ作法）
2. `/kishogenkyo/` 配下（四日市の局番181）に共通のデータ源がないか調べる。あればHTMLパースをやめる
3. `風弱く` `-` `欠測`（§6）の分岐を先に通す。通常値より先にこちらを固めておく
4. `kaze_history.json` の蓄積を回し始める（風配図は24時間たまるまで正しく出せない）
5. `kaze_board.html` を1920×1080で表示してはみ出しがないことを確認。
   `?demo=1` で **風弱く／stale／しきい値超過** の3状態を重ねられるようにする（既存の `?demo=1` と同じ用途）

---

## 6. 未確認・要確認

- [ ] **欠測の表記**。`風弱く` 以外に `欠測` `---` などがあるか未確認（確認した時点では出ていなかった）
- [ ] **`風弱く` の閾値**。何 m/s 未満で `風弱く` になるのかページに書かれていない。
      グラフで0として描くのが妥当かは、閾値がわかってから再判断
- [ ] **2桁風速の表記**。`12 m` なのか桁揃えや別表記があるか未確認（弱風時に確認したため実物を見ていない）
- [ ] **しきい値 15 m/s は仮置き**。実際の運用値を依頼者（＝英典さん）に確認して差し替える
- [ ] **観測値の平均時間**。10分平均なのか瞬間値なのかページに記載がない。
      画面では単に `風速` としてある（`平均風速` と書かない）
- [ ] 名古屋側の観測点が他にもあるか（ページ名が `nagoyako_bw.html` なので `bw` = breakwater の別堤があるかもしれない）
- [ ] **モニタ3へのキオスク起動**。`--window-position` でモニタ3の座標を指定する方式の確認。
      既存ボードの「PC起動時の自動実行の手順確認」と同じ未解決項目なので、まとめて片付けるのが早い
- [ ] 焼き付き対策。暗背景なので有利だが152pxの数字は位置固定だと焼ける。
      数時間ごとに数pxずらすか、深夜に輝度を落とす

---

## 7. 決めた経緯（変えるときに読む）

- **モニタ3に2局だけでも余らない**。1局あたり 現況／最大／平均／推移／風配図 で画面は埋まる。
  無関係なデータで埋めずに字を大きくしたのは、左端モニタが**周辺視**に入るため。
  周辺視は解像度が低く色の面積と動きにしか反応しないので、数字の大きさがそのまま実用性になる
- **風配図を24時間集計にした**理由：6時間だと名古屋24点・四日市12点しかなく、頻度分布として成立しない
- **B案（実況＋予報）／C案（湾内マルチ局）は不採用**。キャンバスのページ「検討時の他案」に残してある。
  余りスペースを埋めるなら、優先順は ①局を増やす ②予報を足す ③無関係なデータ（非推奨）。
  ただし**B案・C案はデータ仕様の確認前に描いたもので、存在しない「最大瞬間風速」を含む**。
  再検討するときはA案と同じ1系列に直すこと
